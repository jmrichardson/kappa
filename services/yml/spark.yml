  spark:
    container_name: spark
    hostname: spark1
    build: spark/.
    image: spark
    network_mode: "host"
    environment:
      - SPARK_CONF_DIR=/conf
      - SPARK_NO_DAEMONIZE=true
    volumes:
      - ./spark/conf:/conf
      - /nfs:/nfs
