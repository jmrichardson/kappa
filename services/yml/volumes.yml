volumes:
  elasticsearch:
    driver: local
  filebeat:
    driver: local
  kafka:
    driver: local
  zookeeper:
    driver: local
  rabbitmq:
    driver: local
  namenode:
    driver: local
  datanode:
    driver: local
  activemq:
    driver: local
  mysql:
    driver: local
